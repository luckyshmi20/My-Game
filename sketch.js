var input

function setup(){
    createCanvas(1580,710); 
    textSize(80);
    textStyle(ITALIC);
    fill("black");
    text("Welcome to the general knowledge quiz", 100, 100);
    this.input = createInput("Enter Your Name Here....")
}
function draw(){
  //  background("lilac");
}